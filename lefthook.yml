pre-commit:
  parallel: true
  commands:
    format-check:
      glob: "*.py"
      run: poetry run ruff format --check {staged_files}
    lint:
      glob: "*.py"
      run: poetry run ruff check {staged_files}